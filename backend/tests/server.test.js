/*const request = require('supertest');
const server = require('../src/server');

describe('Server', () => {
    afterAll(() => {
        server.close();
    });

    it('should respond to /api route', async () => {
        const response = await request(server).get('/api');
        expect(response.status).toBe(404); // Dependendo das rotas que você configurou
    });
});*/
